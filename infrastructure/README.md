# Infrastructure

This folder is reserved for local development infrastructure, database migrations, and deployment support.

Current Goal 1 scope:

- Docker Compose lives at the repository root.
- PostgreSQL is prepared for local development.
- No production deployment or runner isolation is implemented yet.

